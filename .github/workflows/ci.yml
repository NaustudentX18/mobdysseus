name: Android CI

on:
  push:
    branches: [ main, dev ]
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Guard (no symlinks / temp-overlay leftovers)
        run: bash scripts/guard.sh

      - name: Run unit tests
        run: ./gradlew :app:testDebugUnitTest --console=plain

      - name: Lint
        run: ./gradlew :app:lintDebug --console=plain

      - name: Build debug APK
        run: ./gradlew :app:assembleDebug --console=plain

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: mobdysseus-debug-apk
          path: app/build/outputs/apk/debug/app-debug.apk

      # Signed release build. Only runs when a MOBDYSSEUS_KEYSTORE_BASE64 secret is
      # configured (repo keystore is gitignored). Skips gracefully otherwise.
      - name: Build signed release APK
        if: ${{ secrets.MOBDYSSEUS_KEYSTORE_BASE64 != '' }}
        env:
          KEYSTORE_B64: ${{ secrets.MOBDYSSEUS_KEYSTORE_BASE64 }}
          MOBDYSSEUS_KEYSTORE_PASS: ${{ secrets.MOBDYSSEUS_KEYSTORE_PASS }}
        run: |
          echo "$KEYSTORE_B64" | base64 -d > release.keystore
          ./gradlew :app:assembleRelease --console=plain

      - name: Upload signed release APK
        if: ${{ secrets.MOBDYSSEUS_KEYSTORE_BASE64 != '' }}
        uses: actions/upload-artifact@v4
        with:
          name: mobdysseus-release-apk
          path: app/build/outputs/apk/release/app-release.apk
